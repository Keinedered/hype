# 🎓 GRAPH — Образовательная платформа

Современная образовательная платформа с интерактивной визуализацией знаний в виде графа.

---

## ⚡ Быстрый старт (Docker)

```bash
# 1. Запустить полный стек
docker-compose up -d

# 2. Откройте http://localhost:3000
```

**Учетные записи:**
- Админ: `admin` / `admin123`
- Демо: `demo` / `demo123`

База данных инициализируется автоматически!

---

## 🏗️ Стек технологий

| Frontend | Backend | Database | DevOps |
|----------|---------|----------|--------|
| React 18 | FastAPI 0.109 | PostgreSQL 15 | Docker |
| TypeScript | SQLAlchemy 2.0 | 25 таблиц | Docker Compose |
| Vite | Pydantic 2.5 | JWT Auth | Uvicorn |
| Tailwind CSS | Bcrypt | Triggers | PostgreSQL |
| Radix UI | slowapi | Audit Log | 3 контейнера |

---

## 📡 API & Доступ

| Сервис | URL | Описание |
|--------|-----|---------|
| Frontend | http://localhost:3000 | React приложение |
| Backend | http://localhost:8000 | FastAPI API |
| API Docs | http://localhost:8000/docs | Интерактивная документация Swagger |
| Database | localhost:5432 | PostgreSQL (psql) |

---

## 🚀 Docker команды

```bash
# Запуск
docker-compose up -d

# Логи
docker-compose logs -f backend

# Переинициализация БД
docker-compose exec backend python init_db.py

# Остановка
docker-compose down

# Пересборка и перезапуск
docker-compose up -d --build
```

---

## 📁 Основные файлы

```
backend/
├── main.py              # FastAPI приложение
├── models.py            # SQLAlchemy модели (25 таблиц)
├── schemas.py           # Pydantic валидация
├── crud.py              # Бизнес-логика
├── auth.py              # JWT + Refresh tokens
├── exceptions.py        # Кастомные исключения
├── init_db.py           # Инициализация + триггеры
├── routers/             # API endpoints (20+ маршрутов)
└── requirements.txt     # Python зависимости

src/
├── components/          # React компоненты
├── admin/              # Админ панель
├── api/                # HTTP клиент
├── context/            # React контексты
└── types/              # TypeScript типы

docker-compose.yml      # Конфигурация контейнеров
```

---

## 🎯 Основные возможности

**Для студентов:**
- 4 направления обучения (Ивент, Digital, Коммуникации, Дизайн)
- Интерактивная карта знаний (граф узлов и связей)
- Отслеживание прогресса
- Система заданий и проверки работ
- Справочный материал (хендбук)

**Для админов:**
- Управление курсами, модулями, уроками
- Редактор графа знаний (drag & drop)
- Проверка работ студентов
- Аналитика и статистика
- Управление пользователями

---

## 🔐 Безопасность

- JWT токены (30 мин жизни) + Refresh tokens (7 дней)
- Bcrypt хеширование паролей
- SQL Injection защита (ORM)
- CORS контроль доступа
- Аудит-логи всех действий
- Ограничение частоты запросов (rate limiting)

---

## 📊 Архитектура БД

**Основные таблицы:**
- `users`, `tracks`, `courses`, `modules`, `lessons`
- `user_courses`, `user_lessons` — прогресс
- `graph_nodes`, `graph_edges` — визуализация
- `assignments`, `submissions` — задания и работы
- `handbooks` — справочные материалы
- `audit_logs` — полный лог действий
- `refresh_tokens` — управление сессиями

**Автоматизация:**
- PostgreSQL триггеры для синхронизации счетчиков
- Аудит-логи всех изменений данных

---

## 🛠️ Локальная разработка

### Backend

```bash
cd backend
python -m venv venv
source venv/bin/activate  # или venv\Scripts\activate на Windows
pip install -r requirements.txt
python init_db.py
uvicorn main:app --reload --port 8000
```

### Frontend

```bash
npm install
echo "VITE_API_URL=http://localhost:8000/api/v1" > .env.local
npm run dev
```

---

## 🐛 Troubleshooting

**Порт занят:**
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <номер> /F

# Mac/Linux
lsof -i :3000
kill -9 <номер>
```

**Ошибка подключения к БД:**
```bash
docker-compose restart postgres
docker-compose logs postgres
```

**Переинициализация БД:**
```bash
docker-compose down -v
docker-compose up -d --build
```

---

## 📈 Статистика

- **25 таблиц** в базе данных
- **4 трека** обучения
- **6 PostgreSQL триггеров** для автоматизации
- **7 кастомных исключений** для обработки ошибок
- **20+ REST API endpoints**
- **Полный аудит** всех действий пользователей
- **Rate limiting** для защиты API

---

## 🔄 Недавние улучшения (Jan 2026)

✅ Refresh Token механизм для сессий
✅ PostgreSQL триггеры для синхронизации счетчиков
✅ Система аудит-логов
✅ Валидация данных через Pydantic
✅ Rate limiting через slowapi
✅ Кастомная система исключений
✅ Оптимизация запросов (N+1 fix)

---

## 📚 Дополнительно

- **Интерактивная документация:** http://localhost:8000/docs
- **API схема:** http://localhost:8000/openapi.json
- **Исходный код:** GitHub repository
- **Структура проекта:** [Полная документация в кодовых комментариях]

---

**Сделано с ❤️ для образования**
