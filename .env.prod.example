# ============================================
# GRAPH Educational Platform — Production ENV
# ============================================
# Copy this file to .env.prod and fill in real values.
# Used by docker-compose.prod.yml.

# --- PostgreSQL ---
POSTGRES_USER=graph_user
POSTGRES_PASSWORD=CHANGE_ME_strong_random_password
POSTGRES_DB=graph_db

# --- Backend ---
DATABASE_URL=postgresql://graph_user:CHANGE_ME_strong_random_password@postgres:5432/graph_db
SECRET_KEY=CHANGE_ME_run_python3_-c_"import_secrets;print(secrets.token_hex(32))"
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
BACKEND_CORS_ORIGINS=["https://graph.ranepa.ru"]

# --- Frontend (build-time) ---
VITE_API_URL=/api/v1

# --- Domain ---
DOMAIN=graph.ranepa.ru
CERTBOT_EMAIL=admin@graph.ranepa.ru
